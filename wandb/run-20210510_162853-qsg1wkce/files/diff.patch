diff --git a/00-main.ipynb b/00-main.ipynb
index 97b0918..78dc9ab 100644
--- a/00-main.ipynb
+++ b/00-main.ipynb
@@ -541,12 +541,46 @@
     "criterion = nn.L1Loss()"
    ]
   },
+  {
+   "cell_type": "code",
+   "execution_count": 93,
+   "metadata": {},
+   "outputs": [],
+   "source": [
+    "epochs = 100"
+   ]
+  },
+  {
+   "cell_type": "code",
+   "execution_count": 94,
+   "metadata": {},
+   "outputs": [],
+   "source": [
+    "import wandb"
+   ]
+  },
   {
    "cell_type": "code",
    "execution_count": null,
    "metadata": {},
    "outputs": [],
-   "source": []
+   "source": [
+    "wandb.init(project=PROJECT_NAME,name='baseline-0')\n",
+    "for _ in tqdm(range(epochs)):\n",
+    "    preds = model(X_train)\n",
+    "    loss = criterion(preds,y_train)\n",
+    "    loss.backward()\n",
+    "    optimizer.step()\n",
+    "    optimizer.zero_grad()\n",
+    "    loss = loss.item()\n",
+    "    with torch.no_grad():\n",
+    "        preds = model(X_test)\n",
+    "        loss_test = criterion(preds,y_test)\n",
+    "        loss_test = loss_test.item()\n",
+    "    sample_pred = preds[0]\n",
+    "    real = y_test[0]\n",
+    "    wandb.log({'loss'})"
+   ]
   }
  ],
  "metadata": {
